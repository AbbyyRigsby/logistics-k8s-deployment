apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: postgres
  name: postgres
spec:
  selector:
    io.kompose.service: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
